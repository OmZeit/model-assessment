# Lightweight pip install for spreadsheet auditing, reporting, and the local UI.
# Run this file from the `assayready/` project directory. For local-model
# training or tokenizer utilities, install the modeling extra instead:
#
#   python -m pip install ".[modeling]"
#
# The project metadata owns the bounded runtime dependency policy.
.
